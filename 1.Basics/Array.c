#include "stdio.h"
// You can change the library to avoid the %d %s %blah blah
int main(){
    int arr[2];

    arr[0] = 6;
    arr[1] = 23;

    printf("%d" " " "%d" "\n", arr[0], arr[1]);

    return 0;
}